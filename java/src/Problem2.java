import java.util.List;

public class Problem2  {
    /**
     * This function takes a list of rainfall measurements and
     * computes the average rainfall. The function only considers
     * measurements that are greater than or equal to 0. If only
     * negative measurements are given, the function returns null.
     *
     * Arguments:
     *   measurements: A list of measurements.
     *
     * Returns:
     *   The average rainfall as ann integer (round down). If only
     *   negative measurements are given, the function returns null.
     */
    public static Integer raindrops(List<Integer> measurements) {
        // YOUR CODE HERE
        // Return included to allow the skeleton code to compile
        return -1;
    }
}
